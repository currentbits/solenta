# Issues log

- 2026-08-08 - decision: provider `models`/`modelInfo` must be verified from the live CLI (help, `models` subcommand, or local cache like `~/.codex/models_cache.json` / `opencode models` / `kimi provider list --json`), never guessed; `models[i]` and `modelInfo[i]` share id and order; vendor is the model maker not the CLI; empty list is honest when unverified; filling the list turns freeform custom ids into membership checks in setProvider/saveTemplate
- 2026-08-08 - decision: reasoning effort is real only where the CLI accepts it; contract is low|medium|high|xhigh|max; claude `--effort` (all five, unknown values are a silent warning so setReasoningEffort must reject first), grok `--reasoning-effort` (low|medium|high only, CLI errors), codex `-c model_reasoning_effort=` (API enum none|minimal|low|medium|high|xhigh|max → contract intersection all five), kimi/opencode none; prompt is always the last argv element; empty efforts never invent a flag
- 2026-08-07 - symptom: electron tests hang at 100% CPU after runs with session-record | cause: flush while-loop re-entered drain when queue/timer races kept work pending | fix: snapshot queue once in flush (session-record.js)
- 2026-08-07 - symptom: packaged Coder.app loaded vite dev URL / isPackaged false | cause: MacOS binary still named Electron; Electron treats that as unpackaged | fix: rename binary to Coder + CFBundleExecutable in scripts/package-app.sh
- 2026-08-06 - symptom: claude child hung after result; stopAll could not kill it | cause: clearRun on result dropped active handle before process exit | fix: liveClaudeChildren Set reaped by stopAll SIGTERM (runner.js)
- 2026-08-06 - symptom: janitor orphan sweep no-op with a NULL id row | cause: SQL NOT IN fails when subquery contains NULL | fix: NOT EXISTS in janitor.js
- 2026-08-06 - symptom: dev setProvider rejected padded model ids that electron accepts | cause: resolveModel membership used untrimmed String(raw) while electron trims first | fix: trim before membership in devCoder resolveModel
- 2026-08-06 - symptom: opencode run threw workflowProgress flatMap on undefined | cause: pushDetail only skipped __claude/__codex/__kimi, not __opencode | fix: include __opencode in identity guard (electron/runner.js)
- 2026-08-06 - symptom: every thread sidebar age showed "now" forever | cause: store.updateThread stamped updatedAt on every patch (permission mode, sessionId, worktree) | fix: touch option + appendMessage bump; runStartedAt start/clear/recovery (electron/store.js)
- 2026-08-06 - symptom: sidebar age always "now", no live Working elapsed, empty projects missing | cause: formatWorkingLabel used updatedAt (no seconds); groups skipped zero-thread projects; no shared tick; ThreadInfo.runStartedAt unused in dev seed | fix: formatWorkingLabel(runStartedAt) + buildSidebarGroups + 5s tick; devCoder sets/clears runStartedAt
- 2026-08-06 - symptom: non-force removeWorktree deleted branch under detached HEAD with no error | cause: catch defaultBranch and skip unmerged check | fix: WORKTREE_DIRTY + git log -n 10 branch (worktrees.js a18ddbd)
- 2026-08-06 - symptom: second claude turn threw "run already active" after first reached done | cause: result event set status done but left active Map until process exit | fix: clearRun on result event; onExit no-ops via runId guard (runner.js)
- 2026-08-06 - symptom: stop run A then start B; A's late exit marked thread done and cleared B | cause: real-run onChunk/onDone/onError re-looked up active by threadId only | fix: guard e.runId !== closed-over runId in all three callbacks (runner.js)
- 2026-08-06 - symptom: smoke pass B spawn ENOENT for node under Electron | cause: CODER_AGENT_CMD whitespace-split breaks process.execPath under Application Support | fix: smoke resolves space-free node via which/homebrew
- 2026-08-06 - symptom: Thread view mixed all runs into one Work Log above messages | cause: single global card + no runId grouping | fix: buildTimeline in src/timeline.ts; ThreadView merges messages + per-run cards
- 2026-08-06 - symptom: All projects count ignored search filter | cause: section count used unfiltered threads.length | fix: Sidebar.tsx uses filtered.length
- 2026-08-06 - symptom: long chats pushed the composer below the window frame | cause: .app grid had no row constraint, implicit row min-height auto grows with content | fix: grid-template-rows minmax(0, 1fr) in App.module.css
- 2026-08-07 - symptom: every real claude run failed with 'MCP config file not found: <prompt>' when memory was healthy | cause: claude CLI treats space-separated --mcp-config as variadic and swallowed the trailing prompt | fix: single equals form --mcp-config=<path> in getClaudeMcpArgs; caught by the first REAL end-to-end acceptance run, fakes cannot catch argv parsing
- 2026-08-07 - symptom: project-scoped memory recall matched nothing; memory felt global | cause: three key shapes in entries.project (agent cwd paths incl worktrees, app display slugs owner/repo, plain slugs) | fix: canonicalProject = main-repo-root basename applied at every server boundary + normalizeProjectKeys boot migration; electron sends the same key
- 2026-08-07 - symptom: agents reported no memory despite a connected MCP server | cause: headless -p runs cannot approve tool prompts, so every mcp__coder-memory__* call hit permission_denials | fix: --allowedTools=mcp__coder-memory__* (equals form) alongside --mcp-config
- 2026-08-07 - limitation: canonical project keys are repo-root BASENAMES, so two distinct repos with the same folder name (~/work/app and ~/oss/app) share one memory scope, and the boot migration merges such rows irreversibly | rationale: agents can only derive a key from their cwd; a full path would never match the app's own key | revisit if multi-root collisions bite
- 2026-08-07 - practice: fake-CLI tests cannot catch argv-semantics or permission-policy bugs; scripts/acceptance.js (npm run acceptance) spends one real claude turn to prove memory injection, transcripts, run-record and vectors end to end
- 2026-08-07 - decision: memory is PROJECT-SCOPED everywhere (agents included): a query naming project X returns X's rows only, never global or sibling rows | previously the predicate was project-or-global, so global entries leaked into every project's recall and the Memory tab showed the whole machine | unscoped queries (no project given) still see everything, which is what maintenance and cross-project browsing need
- 2026-08-07 - symptom: memory looked ignored inside the app while CLI probes used it fine | cause: out/Coder.app was built BEFORE the --mcp-config equals-form and --allowedTools fixes, so the packaged app failed every memory-injected run | fix: rebuild via npm run package; package-app.sh now stamps buildSha/buildTime into the bundle and main.js logs the build at boot so a stale bundle is identifiable
- 2026-08-07 - decision: memory entries are correctable from the app (Memory tab Edit/Delete): Edit writes a supersession (old row stays as history, superseded_by points forward), Delete removes the row outright and is refused with 409 when another entry supersedes it | rationale: strict project scoping only helps if a wrong entry can be fixed; an agent that read a bad convention keeps reading it until someone can rewrite it
- 2026-08-07 - practice: Settings now answers "is memory healthy" and "which build am I on" without a console (entry/vector counts, last janitor error, version + build sha/time) | rationale: two of the last three production bugs were invisible from the UI (stale bundle, silent janitor failure)
- 2026-08-07 - symptom: editing a memory entry could save a 243-char excerpt over a 980-char body | cause: list rows carry excerpt (recent) or an FTS snippet with [] markers (search), and the Edit button was clickable before the real body arrived, so startEdit fell back to entry.body | fix: Edit is disabled until the full body is fetched; the decision lives in src/memoryCard.ts so it is testable without a DOM
- 2026-08-07 - symptom: Memory tab's default (empty query) list showed every project's rows | cause: memory-proxy recent() built its query from limit only and dropped project, while search() forwarded it | fix: forward project in recent(); this was harmless until round 25 put a hard Delete on every row
- 2026-08-07 - practice: reviewer mutation-testing is the gate that catches vacuous tests: round 25 shipped 4 renderer assertions that all survived their own feature being deleted, plus a build stamp that passed against a hardcoded {x,y,z} | every blocking fix now ships with a mutation that provably fails without it
- 2026-08-07 - symptom: a health probe against an invalid port left a 2s timer that fired into a TDZ ReferenceError | cause: the absolute-deadline setTimeout was armed BEFORE http.get, which throws synchronously on a bad port | fix: arm the deadline after get(); it only worked before because an unrelated try/catch swallowed the error
- 2026-08-07 - practice: an assertion can pass for the wrong reason: the runaway-body test sent garbage, which fails JSON.parse with or without the size cap, so the cap could be deleted with the test still green | the payload must be valid JSON and otherwise healthy so only the cap can reject it
- 2026-08-07 - decision: open GitHub PRs only via the gh CLI (createPr/prStatus), never raw REST or tokens in-process; push() is reused for remote/branch/timeout discipline and origin must be github.com before any gh spawn
- 2026-08-07 - practice: fake gh through CODER_GH_BIN + CODER_FAKE_GH_STATE (same shape as AI CLI fakes); tests must prove non-GitHub remotes and zero-ahead branches never spawn gh, and createPr twice stays at createCount=1 with created:false
- 2026-08-07 - practice: PR badge cannot live inside the ThreadCard button | a nested <a> inside a <button> is invalid HTML and drops clicks | card is a div with a sibling select button and a sibling pr link (src/prUi.ts owns the href decision)
- 2026-08-07 - practice: Git-tab PR form state (title/body/draft/error/live status) must reset on thread.id change only | otherwise row A's failed create error paints onto row B, and a mid-edit title rename would wipe the draft
- 2026-08-07 - decision: renderer PR half is contract-only (git.createPr / git.prStatus / ThreadInfo.prUrl); devCoder is the stand-in until Worker A lands main, and must keep created:false on re-open plus prStatus null when absent
- 2026-08-07 - symptom: a target=_blank PR link would open a bare chrome-less Electron window pointed at github.com | cause: no setWindowOpenHandler or will-navigate policy existed anywhere in main.js | fix: electron/links.js owns the policy (http(s) to shell.openExternal, everything else denied, dev-server navigation still allowed) and main.js only wires it; openExternal on file:/javascript:/data: URLs is a local-execution footgun, so the scheme check is the load-bearing part
- 2026-08-07 - practice: worker scope boundaries need enforcing at integration, not trusted: the electron worker also edited src/devCoder.ts (the renderer worker's file), and the renderer version was the one to keep
- 2026-08-07 - symptom: the whole renderer half of a feature could be deleted with a type-clean two-line edit and the suite, tsc and vite build all stayed green | cause: pure modules were tested but nothing rendered a component, so wiring was untestable by construction | fix: test/support/render-hooks.mjs (esbuild for .tsx, Proxy stub for CSS modules, both already available) plus renderToStaticMarkup assertions; npm run test:renderer runs .test.ts and .test.tsx together
- 2026-08-07 - symptom: IPC channel renames shipped a broken app with a green suite | cause: tsc cannot check channel-name strings, and nothing loaded ipc.js and preload.js together | fix: electron/test/ipc-seam.test.js stubs the electron module and drives a real createPr round trip through the preload surface
- 2026-08-07 - symptom: selecting threads froze the entire app for up to 30s each | cause: an automatic prStatus effect fired on every thread selection, and gh runs through execFileSync in the main process, blocking the event loop including agent streaming | fix: only refresh when the thread already has a prNumber; the create path does its own lookup
- 2026-08-07 - symptom: every GitLab or remote-less project showed a permanent red error in the Git tab | cause: prStatus throws for a non-GitHub origin and the effect surfaced it as role=alert | fix: absence of a GitHub remote is a state, not an error; the effect falls back quietly and createPr still explains on click
- 2026-08-07 - decision: only an OPEN pull request short-circuits createPr, and the Git tab keeps the create form available when the known PR is CLOSED or MERGED | gh pr view returns finished PRs too, so accepting them left a branch unable to open a follow-up | this rule has THREE sites (the first lookup, the post-failure race lookup, and prUi.showForm) and was shipped at one of them twice before it was true end to end
- 2026-08-07 - symptom: a failed gh pr create could resolve successfully and stamp the store with a stale merged PR | cause: the race path re-views after a failed create and returned a PR in ANY state, which the merged-PR fix newly made reachable | fix: the race lookup applies the same OPEN filter as the first one; a fix that is correct at one site must be applied at every site that reads the same value
- 2026-08-07 - decision: livePr uses undefined for "unknown, fall back to the recorded PR" and null for "confirmed no PR" | a failed status refresh must set undefined, not null, or the Git tab shows a create form for a thread whose PR is visible in the sidebar
- 2026-08-07 - practice: to test that a component is WIRED, render the parent that owns the call site | exporting the child does not catch a mutation at the call site, since the child is never asked to render
- 2026-08-08 - practice: never wrap an assertion in a conditional that its own failure mode would make false | a test reading `if (html.includes("Create PR")) { assert(...) }` passes silently when the button disappears, which is exactly the regression it was written for
- 2026-08-08 - symptom: long branch names hid the sidebar PR chip behind .branch ellipsis; card used two select buttons so meta row clicks missed and Tab was a maze | cause: PR <a> nested inside overflow:hidden .branch; cardSelect only wrapped title, metaSelect only wrapped the badge | fix: branchRow flex (truncate .branch only; PR chip flex-shrink:0 sibling); one empty stretch .cardSelect (inset:0) with .cardBody pointer-events:none and .prLink pointer-events:auto so meta clicks select without nesting interactive elements
- 2026-08-08 - symptom: long branch names made the sidebar PR badge disappear | cause: round 26 nested the PR link inside `.branch`, which has overflow:hidden + text-overflow:ellipsis, so the badge was clipped at the end of the mono line | fix: `.branchRow` flex row with branch-only ellipsis and a flex-shrink:0 PR sibling (Sidebar.tsx / Sidebar.module.css); render test asserts the branch span is branch-only
- 2026-08-08 - symptom: collapsing a Memory card mid-edit silently discarded the typed draft, and actionError/editId/confirmDeleteId were single un-keyed slots so card A's error or confirm could paint on card B | cause: toggleExpand called clearCardActions() which nulled the draft, and per-card UI state was global scalars | fix: CardActionsById maps keyed by entry id in memoryCard.ts; afterCollapse keeps draft+editing (only drops confirm chrome); MemoryTab reads only the open entry's keys
- 2026-08-08 - symptom: renderer tests failed on a fresh clone with "Cannot find package esbuild" | cause: the round 26 render harness imports esbuild, whose postinstall is blocked by this machine's npm allowScripts policy, so it only worked where vite had already built the binary | fix: allowScripts esbuild in package.json; a harness that depends on a native binary must declare it
- 2026-08-08 - practice: a mutation that only renames a CSS-module class proves nothing, since the test stub returns the key as the class name and structural assertions do not read it | to prove a layout fix bites, move the ELEMENT (re-nest it), not the class
- 2026-08-08 - symptom: a CSS assertion passed against code that had the declaration deleted | cause: cssRuleBody returned the rule body including comments, and the comment explaining WHY min-width:0 matters contains the text "min-width:0" | fix: strip comments in cssRuleBody; an assertion that can be satisfied by prose about the code is not testing the code
- 2026-08-08 - decision: memory drafts survive collapse and re-expand but NOT a list reload | a successful save or delete calls reloadCurrent which clears every card's action state, so an untouched draft on another card is lost too | this is what prevents an id-keyed draft map from growing forever
- 2026-08-08 - practice: renderToStaticMarkup plus a CSS-module stub cannot see layout | round 27's PR-chip fix was verified green by the harness while a flex-grow left the chip stranded 83px from the branch text, found only by rendering the real component with the real stylesheet in a browser and measuring
- 2026-08-08 - symptom: a commented-out CSS rule could hide a real declaration from the test that asserts on it, shipping a dead PR link green | cause: cssRuleBody stripped comments from the SLICE, after the rule lookup had already searched the raw file, so a comment shaped like a rule hijacked the lookup and a brace inside a comment truncated the body | fix: strip comments before locating the rule; a comment must never be able to satisfy OR defeat an assertion about the code
- 2026-08-08 - symptom: in a jsdom test a controlled input updated in the DOM but onChange never fired, so component state stayed stale and a save silently wrote the OLD value | cause: ES imports are hoisted, so `import "react-dom/client"` evaluated before the test harness installed the jsdom globals, and react-dom bound its event system to a documentless world | fix: install one jsdom for the process and import react-dom dynamically inside mount(), after the globals exist
- 2026-08-08 - practice: test/support/dom.ts mounts components in jsdom so effects and clicks run; test/support/render.mjs renders to static markup and runs neither | use the static one where markup is enough (far cheaper) and mount() for anything driven by effects, state or interaction
- 2026-08-08 - practice: a failing assertion skips the test's own unmount, and MemoryTab keeps a 60s interval, so one red test used to hang the entire run instead of failing | mount() registers every instance and tests call unmountAll() from afterEach
- 2026-08-08 - practice: ThreadView only paints work-log STEP bodies when that card is open (defaultOpen is the latest run only), so a static render of two runs shows two "Work Log" titles but only the latest steps | assert open steps or mount+expand; do not expect collapsed step labels in markup
- 2026-08-08 - practice: ThreadView auto-expands a tool card only when that tool's runId matches a work-log group (latestRunningToolId); a lone tool message with no work log stays collapsed until click
- 2026-08-08 - practice: Composer had zero render coverage; model pick, permission mode, and Send/Build are state-driven so only mount() (not renderToStaticMarkup) can catch a silent break | round 28b adds test/composer.test.tsx; Composer has no token/cost prop (spend lives on Sidebar) and Stop lives on ThreadView, so the active-run gate is the disabled prop alone
- 2026-08-08 - practice: controlled <input value=...> text is NOT in textContent; mount().text() misses phase names and similar fields, so assert on (el as HTMLInputElement).value (or the attribute) or the assertion is vacuously green when the field disappears
- 2026-08-08 - practice: SettingsModal budget validation is backend-owned (Number(raw) then services reject); empty means no-cap null, and type=number blocks pure non-numeric keystrokes before state changes, so a "rejects non-numeric" render test must exercise the onSaveSettings rejection path, not only typed letters
- 2026-08-08 - practice: test/support/dom.ts registers its own afterEach cleanup and fails a test on any React console.error | relying on each file to remember afterEach(unmountAll) meant one forgotten line wedged the whole run with no output (--test-timeout bounds a test, not the run), and React reports key warnings, act warnings and controlled/uncontrolled switches only through console.error | node's own DeprecationWarning arrives on the same channel and must be filtered or every run fails on unrelated noise
- 2026-08-08 - practice: a stub that ignores its arguments cannot catch a wrong-argument bug | MemoryTab tests passed while the component dropped the project scope, fetched the wrong entry id, and wrote a correction onto a different entry, because the stubs only counted calls | capture every argument and assert on it
- 2026-08-08 - practice: assert on the element you mean, not on tab-wide text | includes("convention") was satisfied by the store form's <option>, so the type badge could be deleted from every card with the suite green
- 2026-08-08 - practice: a for-loop assertion needs a cardinality guard or it passes hardest when the component renders nothing | four structure tests iterated over queryAll("button, a") and passed 1/0 with the component deleted at its call site, which is the exact bug the tests existed to catch
- 2026-08-08 - symptom: a regex depth counter for nested interactives could be defeated by a stray close tag | cause: depth went negative, so a genuinely nested pair later in the document read as depth 1 | fix: floor at zero
- 2026-08-08 - practice: anchor every alternative in a filter regex | /^\(node:\d+\)|DeprecationWarning|ExperimentalWarning/ anchors only the first branch, so the console-error gate silently swallowed any React warning whose text contained "DeprecationWarning"
- 2026-08-08 - practice: never let a comment claim an assertion exists | a fix for an over-claiming comment replaced it with one saying "asserted, not just asserted-in-a-comment" while the assertion was still missing, which is strictly worse: the next reader has no reason to look
- 2026-08-08 - decision: model picker is a two-pane popover (list + detail/reasoning); empty ProviderInfo.efforts hides the reasoning meter entirely (not disabled); trigger and rows show ModelInfo.label with raw-id fallback; pure helpers live in src/modelPicker.ts; decorative "High · 1M" pill removed so capacity is not faked next to a real control
- 2026-08-08 - decision: ReasoningEffort is low|medium|high|xhigh|max (no minimal); the meter is one segment per ProviderInfo.efforts entry only (claude five, grok three, empty = no control), never hardcoding REASONING_EFFORTS; offering an unsupported level is silent on claude (warning + default), so the picker must not invent levels
- 2026-08-08 - symptom: switching provider left a reasoning level the new provider cannot honour, shown in the picker and never sent to the CLI | cause: setProvider cleared model on a provider change but not reasoningEffort | fix: clear both; a level displayed to the user that reaches nothing is the exact bug this feature removed one control to the left
- 2026-08-08 - practice: a nested-interactive guard must assert the surface it guards is OPEN | the composer test opened the model popover first, and every other menu click closed it, so the two-pane picker was absent by the time the guard ran and nesting inside it went unnoticed
- 2026-08-08 - practice: "key in args" is not an argument assertion | a memory-scoping test asserted the project key existed rather than its value, so the app could scope memory to a project that does not exist with the suite green
- 2026-08-08 - practice: when a fix lands in electron/services.js, check src/devCoder.ts for the same branch | the dev harness carries a comment claiming it matches setProvider exactly, and it did not, so npm run dev reproduced the stranded-effort bug the shipped path had just fixed, which reads as the fix not landing
- 2026-08-08 - symptom: a freshly packaged app showed the PREVIOUS round's UI while its build stamp read the new commit | cause: package-app.sh only ran vite build when dist/ was absent, so it copied a stale dist and then stamped the CURRENT git sha on top, making the stamp lie | fix: always rebuild dist/ and core/dist when packaging; a stamp derived from git rather than from the artifacts cannot detect a stale artifact, which defeats the reason the stamp exists
- 2026-08-08 - decision: one unified model picker lists every provider's Default + models; selecting a row sets provider and model together via setProvider; unavailable rows and session-locked other-provider rows are disabled (not hidden); the reasoning meter follows the highlighted row's provider efforts; pure decisions live in modelPicker.ts (buildUnifiedModelRows, stepHighlightIndex)
- 2026-08-08 - decision: a provider's models list is a SUGGESTION, not an allowlist | it is a snapshot of a CLI's catalogue and goes stale the moment a model ships, so rejecting an unlisted id blocks a user from a model their CLI already supports, and once every provider published a list it also made the picker's Custom option dead everywhere | length and emptiness guards still apply, and a bad id fails loudly at the CLI, which is the layer that actually knows
- 2026-08-08 - symptom: codex and opencode showed only "Default" in the dev picker while the packaged app listed their models | cause: src/devCoder.ts carries its own provider list and worker changes to electron/providers.js do not reach it | fix: synced dev from the real registry; anyone tuning the picker in dev was seeing different data than ships
- 2026-08-09 - symptom: adding one row to the model list made a test file hang for 45s and fail with no detail, while every test in it passed individually in 30ms | cause: a test hardcoded "three ArrowDowns lands on the next provider", so a longer list silently pointed it at a different row and the failure interacted badly with the runner | fix: assert the RULE (arrow until the highlight leaves the provider) rather than a magic count; a positional assertion starts testing something else the moment the list changes
- 2026-08-09 - practice: a fixture can make a rule untestable | the only multi-effort non-current provider was available:false, so arrow navigation skipped its rows and the cross-provider reasoning case could not be reached by any test, leaving three guards unprotected
- 2026-08-09 - decision: a model list's Custom row is what makes "lists are suggestions" reachable | without it the loosened validation had no UI path and bought users nothing, which is how the round 31 policy change nearly shipped inert
- 2026-08-09 - symptom: a workflow phase validated a 101-char model id and then stored 102 characters | cause: saveTemplate called normalizeModelForProvider for its throw and discarded the return, then persisted String(p.model) | fix: store the normalized value; validating a value and persisting a different one defeats the guard it just passed
- 2026-08-09 - practice: "arrows still work" does not test focus | press(el, key) dispatches on the element it is handed, so a test passes whether or not anything is focused; assert document.activeElement when the behaviour under test IS focus
- 2026-08-09 - symptom: reopening the model picker showed a custom-model text field aimed at a provider the user had highlighted minutes earlier, on a different thread | cause: customFor was cleared only by commit and by the field's own Cancel/Escape, so every other close path (outside click, Escape with focus elsewhere) left it set while the field looked dismissed | fix: reset it when the popover OPENS, which covers every close path including ones added later
- 2026-08-09 - decision: the model picker drills (providers, then that provider's models) rather than listing every model flat | the flat list reached 26 rows, which is more than a popover shows and more than a user reads; drilling shows five rows to start and one harness's models after that
- 2026-08-09 - symptom: a locked thread rendered the full lock sentence on every provider row, wrapping to three lines each | fix: rows carry a short badge (locked, not installed, "4 models") and the sentence moves to the tooltip; a row is not a place for prose
- 2026-08-09 - practice: when a UX change forces a test rewrite, diff the test NAMES before and after | replacing a describe wholesale silently dropped four behaviours (raw-id fallback, hiding the meter for a provider with no efforts, per-provider segment count, and reporting a clicked level) because the remaining suite still passed
- 2026-08-09 - practice: a test that closes a menu by stepping back first cannot prove state resets on close | the drill level was cleared as a side effect of stepping back, so the reset could be deleted with the test green; close from the deepest state instead
- 2026-08-09 - symptom: the model picker was keyboard-dead after any level change, and Escape closed it instead of stepping back | cause: focus was set only on the popover's open edge, so the list that had focus unmounted on drill-in and drill-out and focus fell to <body>; the keydown then never entered React, so the handler that stops Escape propagating was never reached | fix: focus the list that is actually showing, keyed on the level
- 2026-08-09 - practice: press(el, key) dispatches on the element it is handed, so it cannot detect lost focus | test/support/dom.ts now has pressFocused(key), which throws unless something inside the tree is focused; any claim of keyboard operability should go through it
- 2026-08-09 - practice: a fixture where the current item is also index 0 makes "highlighted", "current" and "first" indistinguishable | four separate navigation bugs survived mutation because the thread's provider was PROVIDERS[0]
- 2026-08-09 - symptom: with the picker on the provider level the detail pane described an unrelated model, e.g. a Grok thread showed "Fable, Anthropic" | cause: the pane indexed the flat model list with the MODEL-level highlight index, which at the provider level points at whatever it happened to be | fix: providerDetail() describes the highlighted provider; a pane that confidently describes something the user is not pointing at is worse than an empty one
- 2026-08-09 - practice: a disabled row is deliberately not hoverable | React suppresses onMouseEnter, onClick, onMouseDown, onMouseMove and onMouseUp on disabled interactive elements (onMouseOver and onMouseOut still fire, so this is not a blanket rule), which means a disabled provider cannot be highlighted; its row text and title carry the reason instead
- 2026-08-10 - symptom: after backing out of a provider, the user's OWN reasoning meter went dead: the pane said current harness at the live level while every segment refused the click | cause: a migration from detailRow to a level-aware detail stopped two lines short, leaving aria-pressed and disabled reading the stale row | fix: finish the migration; a partial migration is worse than none, because pane and control then disagree and the UI contradicts itself
- 2026-08-10 - practice: verify a FIX applied, not just a mutation | a whitespace-mismatched replace left both lines untouched and the suite still passed, so the "fix" was reported green while the bug was live; count occurrences before and after
