# solenta

Standing instructions generated from Solenta shared memory — conventions, strategies, and verified decisions. Prefer these over restating the tree.

<!-- generated-by: solenta-config-doctor -->

## Conventions

### Site design language: paper-and-ink, yellow marker highlight, dark only as product artifacts

As of commit 78fe3bf (2026-08-20) the solenta.app site is LIGHT, not dark: off-white paper (#f5f5f2), ink text (#191918), one high-vis yellow accent (#f2e51f). The yellow is used as a marker-highlight stripe behind key headline words (.em class, animated sweep on reveal), as the CTA/button fill, and as one full-yellow band (section#agents). Dark surfaces (--panel #131312) appear ONLY as product artifacts: screenshots, the memory-panel mock, the prompt card, install terminal blocks. Docs/changelog inherit via CSS variables. Do not reintroduce: dark page theme, pointer-glow/spotlight cards, blurred drifting blobs, pulsing status dots, fake macOS window bars, or em-dashes anywhere in visible copy (all were removed deliberately; visible copy is em-dash-free). The previous dark design lives at
…

### Memory bodies are published: CLAUDE.md/AGENTS.md go to a PUBLIC repo

currentbits/solenta is a PUBLIC GitHub repo, and CLAUDE.md + AGENTS.md are committed there. Both are generated by the config doctor (electron/configDoctor.js), which templates memory entry bodies in near-verbatim: every `convention` and `strategy` entry always, plus `knowledge` at importance >= 3 or with citations (caps 20/10/15, bodies truncated at ENTRY_BODY_CAP 800). There is no redaction step and no private flag.

So anything you write into a convention/strategy/knowledge body is a publication. Keep out of those bodies:
- names of the author's OTHER repos (the round-29 entry leaked four of them in a per-project worktree table until 2026-08-20)
- absolute paths containing the username — write ~/code/... or a repo-relative path
- local file drops like ~/Desktop/<file>

Measurements about
…

### Solenta release tweets: 0.4.0 shape, never a changelog dump

Willem posts Solenta updates as himself (@currentbitsNET), not as a product blog. The tweet that matches his voice is the 0.4.0 one (2026-08-16): one casual first-person line, then New/Perf/Fixed bullets, then a short closer. The 0.6.0 draft was a press release plus a WHAT WE SHIPPED appendix; he pasted that whole thing and later called the same shape for 0.7.0 a terrible tweet.

Do:
- casual opener, lowercase ok
- 5–8 short bullets of things you feel
- one 'also:' leftover line
- changelog URL, no appendix, no ALL CAPS section headers, no MIT/local-first closer, no '29 PRs'

Do not recreate the 0.6.0 press-release shape.

### Runner fixtures that bind a worktree must pass userDataPath

createRunner defaults userDataPath to "". After #511, startRun calls materializePendingWorktree for any thread with pendingWorktree or worktreePath, and throws `worktreeBase is not configured` when userDataPath is empty. The rematerialize path is path.join(userDataPath, "worktrees").

Any electron test that setupWorktree's into tmpDir/worktrees and then startRun MUST pass userDataPath: tmpDir (see worktree-setup-fail.test.js, worktree-cleanup.test.js, verify-gate.test.js, checkpoints.test.js). Checkout-only runner tests (no worktreePath) can omit it.

### Sidebar renders buildFlatSidebar (T3 flat), not partitionSidebar — the #567 Active+Later contract is superseded

The standing rule "Sidebar #567: Active + Later shelf contract replaces four-shelf model" is now itself stale. As of 011a629 (2026-08-20) src/components/Sidebar.tsx imports ONLY `buildFlatSidebar` from ../sidebarGroups (line 31). `partitionSidebar` and `flattenLater` still exist in src/sidebarGroups.ts but are referenced nowhere in src/ outside their own file — only test/sidebarGroups.test.ts and test/threadSnooze.test.ts still call them. They are dead app code kept alive by tests.

Current model (buildFlatSidebar, src/sidebarGroups.ts:163): a T3-style FLAT sidebar with NO project group headers. Precedence archived > snoozed > pinned > settled > active, and pinned is its OWN top section (sorted oldest-first via comparePinnedOldestFirst) rather than sorting first inside a project group. sno
…

### Solenta release checklist: worktree build, curated notes, girder graft

Full "cut a release" sequence, run end to end for v0.6.0 (2026-08-17, 60 commits since v0.5.0):

1. `git pull --ff-only origin main` FIRST. The main checkout often carries uncommitted WIP; stash it by path (`git stash push -m ... <file>`) rather than committing it into the release, and `git stash pop` after. publish-release.sh refuses a dirty tree anyway.
2. Build the change list from `gh pr list --state merged --json number,title,mergedAt` filtered on mergedAt > the previous tag's date, NOT from merge-commit subjects — worktree merges have useless subjects. Verify the boundary: PRs merged just before the tag still show up in that window, so check `git merge-base --is-ancestor <mergeCommit> <prev-tag>` for the borderline ones (three of them were already in v0.5.0).
3. Bump `package.json` v
…

### Skill dirs are symlink farms: scan with isSymbolicLink, copy with dereference

Willem's real setup already fans skills out by hand: ~/.claude/skills holds 28 SYMLINKS into ~/.agents/skills (only 3 real dirs). Any scanner using readdirSync(..., {withFileTypes:true}) + d.isDirectory() sees ZERO of them — dirent.isDirectory() is false for a symlink, it does not follow. The skill manager (#343, electron/skills.js scanSkillDir) shipped that bug past a fully green suite; only running listSkills against the real HOME caught it. Not cosmetic: all 31 skills read as missing from claude, so Sync would have cpSync'd ~130k of content over the symlink farm.

Two rules for anything walking skill dirs:
1. Accept `d.isDirectory() || d.isSymbolicLink()`; the follow-up fs.statSync(<dir>/SKILL.md) follows the link, and a dangling link just throws into the existing catch.
2. fs.cpSync(sr
…

### Memory scoping: always pass the project PATH, never the display slug

The memory server canonicalizes project keys to the basename of the MAIN repo root (memory-server/src/project-key.js): a path like /Users/willem/code/coder -> "coder", and worktrees resolve to the same key via git-common-dir. A display slug like "currentbits/solenta" canonicalizes to "solenta" — a scope no agent ever writes to. This is exactly why the solenta project's Memory tab showed empty while other projects (slug == dir basename) worked. Fixed 2026-08-14 (commit 87af397): AgentsPanel now passes project.path to MemoryTab. Rule for any new memory call site in the app: send the raw repo path and let the server canonicalize; never send project.slug or project.name.

### Website changelog never uses issue numbers

site/changelog.html entries (0.2.1 through 0.8.0) never include (#123) issue numbers. User-facing bullets with a bold feature name, then a sentence. Added / Fixed (rarely Security). When cutting a site changelog, drop issue numbers even if the GitHub release notes keep them.

### Worktree electron tests also need core/dist and memory-server/node_modules

A plain git worktree plus ln -s main/node_modules and core/node_modules is not enough for npm run test:electron. core/dist is gitignored (built artifact) and memory-server/node_modules is a separate install. Without them the suite reports hundreds of MODULE_NOT_FOUND (core/dist/index.js) and orch-server failures (MCP SDK missing, coder-threads binds port null). Also symlink:
  ln -sfn /Users/willem/code/coder/core/dist core/dist
  ln -sfn /Users/willem/code/coder/memory-server/node_modules memory-server/node_modules
#536's 7 failures (verify-gate 0/4, checkpoints, web, store) were not reproduced on this tree once those links exist.

### Renderer tests: drainConsoleErrors() for deliberate component crashes

Solenta's test/support/dom.ts fails the whole suite if React logs ANY console.error while a component is mounted (unmountAll gate). Tests that deliberately crash a component (e.g. test/errorBoundary.test.tsx, issue #81) must call drainConsoleErrors(/pattern/) after the crash to acknowledge the expected noise; non-matching errors still fail the gate. The app itself now wraps the three main panes (Sidebar / Thread view slot / AgentsPanel) in src/components/ErrorBoundary.tsx, each with Try again (reset boundary) and Reload app (window.location.reload, injectable via onReload prop for tests).

## Decisions and gotchas

### Round 36 (2026-08-20): failure taxonomy mined from live transcripts — #626 grok config self-corruption is the only live failure class

Round 36. New method, high yield: mine the live store's EVENT messages for a real failure taxonomy, then date-bucket each class to separate fixed-and-dead from currently-live.

METHOD (reusable, ~3 minutes): read coder-store.json, filter messagesByThread for role==="event", regex the text for error/fail/stopp/cancel/timeout/denied/quota, normalize (strip hex ids and digits) and tally. Then bucket each class by day from m.createdAt. thread.lastError is USELESS for this — it is cleared on the next turn, so all 549 threads carry zero. The transcripts are where failure history actually lives.

TAXONOMY (549 threads, 961 event messages, 2026-08-13..20):
- Run stopped              108  08-13..08-20 (user steering, peak 50 on 08-17 — behavioural, not a defect)
- error_during_execution    44  08-1
…

### Round 34 (2026-08-20): verify is broken on desktop (#622) — preload drift the seam test cannot see; six-place IPC declaration (#623)

Round 34, perf + refactor. The refactor angle immediately produced a live product bug.

#622 — SHIPPING BUG, desktop only. coder.threads.setVerifyCommand and coder.threads.runVerify are UNDEFINED in the packaged app. electron/preload.js contains no occurrence of "verify" at all. Proven by loading the real preload against a stubbed electron: threads.setVerifyCommand = undefined, threads.runVerify = undefined, 39 threads methods present. Every OTHER layer has them — ipc.js:649,654 (handler), wireClient.ts:442-443 (web), devCoder.ts:3216,3225 (browser dev), useCoder.ts:2496,2509 (renderer call), App.tsx:195-196 -> AgentsPanel.tsx:1966,2979 (UI). resolveCoderApi (src/coderApi.ts) is whole-object (`if (w.coder) return w.coder`), NOT per-method, so a packaged build returns the bridge and throws
…

### Grok 1.0.5 --permission-mode auto is default and cancels bash as user stop

On grok CLI 1.0.5, `--permission-mode auto` is a no-op: the streaming-messages-json init event reports permissionMode=default. Headless `-p` then auto-cancels gated tools (run_terminal_command) with `User cancelled the execution for tool …` and errors:["cancelled"]. Solenta #549 maps that to idle + "Run stopped", which looks like the user hit Stop.

`--always-approve` init-reports bypassPermissions and actually runs bash. Asking modes (default, acceptEdits) must map to `--permission-mode bypassPermissions` plus `--always-approve`, not auto. plan still passes through.

Issue #578. Live-verified 2026-08-19: auto+list_dir succeeded; auto+bash cancelled; always-approve+bash succeeded.

### Perf A/B of the 2026-08-21 build: CPU halved, memory flat, FICLONE a no-op

Controlled A/B of electron/store.js at 80b88d3^ (before) vs 9416232 (after: #638 markDirty, #637 FICLONE, #225 interim flush cap 2s->15s). Harness loaded both modules into node against their own clone of the live coder-store.json (181.3 MB), one store.save() every 100 ms for 30 s, 3 interleaved runs each, same machine.

MEDIANS (before -> after):
- CPU 25.4% -> 12.4% of one core (-51%). Real win, tight spread (25.2/27.6/25.4 vs 12.5/12.3/12.4). It is the 15s debounce cap halving whole-store stringify+write cycles.
- RSS after boot 1127.9 -> 1127.6 MB (0%). Peak RSS during a flush 2197.7 -> 2191.7 MB (-0.3%, noise). NO memory win shipped; do not claim one. Peak RSS ~2x resident because JSON.stringify of the 181 MB store materialises a second copy.
- Boot load + .bak snapshot 709 -> 691 ms (
…

### Research round 39 (2026-08-21): worktree regrowth to 48 GB = nightly predates #624 by 82 min; filed #641 periodic sweep, #642 corrupt-dir GC

Round 39 (thread 80909445), disk/background-work angle. Filed #641 + #642, commented verification evidence on #624.

HEADLINE: worktrees regrew 29 GB → 48 GB (62 dirs, 61 belonging to archived threads) because the #624 transient-reclaim fix merged at 20:35 on 08-20 and the installed nightly was built 19:13 — missed by 82 minutes (grep the bundled app for isTransientWorktree = 0). Not a code regression on main. After a fresh nightly, 19 dirs are immediately reclaimable (past grace, 18 clean); 42 sit inside the 3-day UNMERGED_GRACE_MS window.

FILED:
- #641 — enforceRetention runs only at boot (+15s, main.js:380) and on archive/merge. No periodic timer, so grace crossings during multi-day uptimes are never reclaimed. Fix: 6h unref'd interval, same pattern as createPrStateRefresher.
- #642 —
…

### Research round 38 (2026-08-21): filed #639 boot parse, #640 generic-path throttle; store is 84% tool cards

Round 38 (thread 80909445), second half of the perf/CPU research pass. Filed #639 and #640; everything else audited was already fine or already tracked.

FILED:
- #639 — Boot parse: replaying the live store load costs 19 ms read + 581 ms JSON.parse (183 MB file) synchronously on the main process before any IPC answers (electron/store.js:1019 → _readFile:1032). #618 fixes window ordering but not this payload. Fix: lazy-hydrate messagesByThread per thread, or worker-thread parse.
- #640 — Generic provider onChunk (electron/runner.js:2333-2347) is unthrottled: every raw stdout chunk → updateMessage (copy-on-write slice+findIndex, store.js:1851) + save() + pushDetail (getThreadDetail + firstChanged + IPC). Codex (runner.js:3762) and kimi/opencode (4142) already have throttledPush; generic neve
…

### Research round 37 (2026-08-21): idle store writes verified fixed; streaming flush = 16 MB/s is the only live perf cost, all tracked in #225

Round 37 (this thread 80909445), continuing the dogfood-the-machine method. Verified the #636/#637/15s-debounce interim (commit 755f0a2) live and re-quantified the residual.

MEASURED (nightly, 2026-08-21 ~18:40):
- Store now 192 MB (143 on 08-19, 180 on 08-21 AM): ~18-20 MB/day growth, messagesByThread = 181.4 MB across 617 threads, largest thread only 2.3 MB/1073 msgs, zero messages >200 KB — growth is uniform medium messages, no blob offender.
- IDLE IS FIXED: 0 rewrites in 30 s idle; main (pid sampled 5 s), renderer, GPU all idle in sample(1). Click/boot flushes gone.
- STREAMING residual: 4 full flushes in 45 s during one streaming thread = 733 MB written (~16 MB/s sustained); each flush is a 192 MB JSON.stringify on the main thread (electron/store.js:1193). Debounce backoff reaches ~
…

### Merge gate: question-card answer resets isAutoTurn; merged workers look worktree-less

Two facts about the orchestration merge gate (#614 / commit 862812a):

1. Store forensics trap: thread_merge cleanup deletes the worker's worktree and clears worktreePath + branch on the thread record, so a merged worker is indistinguishable from one that never got a worktree when reading coder-store.json. Do not conclude "worktrees are not created for orchestration" from wt=null rows — check the lead's transcript for the [orchestration] fork event and cd's into worktrees/<workerId> instead.

2. assertUserApproved (electron/orchServer.js) refuses thread_merge/thread_pr while runner.isAutoTurn(leadId) is true (notice-delivered turn chain). Fixed gap (commit 5057b1a, branch coder/when-we-run-tasks-with-the-orhcestration-89b55b): respondPermission in electron/runner.js now zeroes the autoTurn
…

### Research round 29 (2026-08-19): dogfood the live store, not the competitors — 2 issues, 2 comments

Round 29. After 28 competitor rounds the external vein is dry; the empirical vein is not. Method that worked: read-only scan of ~/Library/Application Support/Solenta (coder-store.json + worktrees/) and compare against what shipped.

NOTE: this entry is templated verbatim into CLAUDE.md / AGENTS.md by the config doctor, and those files are committed to a PUBLIC repo. The per-project worktree breakdown that used to be here named the author's other private repos; it has been generalized. Keep it that way — see the "memory bodies feed a public repo" convention.

MEASURED ON THE LIVE MACHINE (2026-08-19, 445 threads, 5 projects):
- worktrees/ = 96.0 GB across 166 dirs, median 709 MB, 161 belong to archived threads, only 4 working, 0 pinned. worktreeRetention is undefined on every project, so en
…

### Research round 33 (2026-08-20): full suite baseline is green except one load-dependent flake; shared review-itinerary path (#621)

Round 33. Completed the suite picture and mined a workflow defect I hit myself.

FULL BASELINE on main 5b1c6da (2026-08-20) — all four suites plus typecheck, run for real:
- npm run typecheck (tsc --noEmit): exit 0, clean
- test:core       38 pass / 0 fail  (12 suites, 0.06 s)
- test:renderer  1338 pass / 0 fail (368 suites, 23 s)
- test:memory     201 pass / 0 fail (55 suites, 1 s)
- test:electron  ~1683 pass / 0-1 fail (352 suites, 67 s)
That is the whole verification surface. Nothing else is red.

FLAKE RATE IS LOAD-DEPENDENT (added to #620): the web-integration reconnect test fails 1/5 in isolation but 3/4 under the full electron suite, because CPU contention makes the socket round-trip likelier to lose to the immediately-fired abort timer. CI always runs loaded, so CI sees it red most
…

### The "known failing electron test" is a fixable race, not a baseline (#620)

SUPERSEDES the guidance that `fail 1` on web-integration reconnect is an acceptable baseline on main. It is a flaky test with an identified root cause, fixed and verified in #620.

MEASURED on main 5b1c6da (2026-08-20): npm run test:renderer = 1338 pass / 0 fail (368 suites, ~23 s). npm run test:electron = 1681 pass / 1 fail (352 suites, ~67 s). The 7 failures of #536 and the 4 of #535 are all genuinely gone; #535 is stale and should close in favour of #620.

ROOT CAUSE: electron/test/web-integration.test.js:301 injects a fake setTimeout that returns `setTimeout(fn, 0)` for EVERY timer once allowReconnect flips true. But wireClient routes both timer kinds through the same seam (`const schedule = opts.setTimeout ?? setTimeout`, src/wireClient.ts:94): scheduleReconnect's ~1000 ms backoff (wi
…

### Research round 31 (2026-08-20): threads:changed payload (#617) and boot ordering (#618); store growth doubling

Round 31, performance, continuing the dogfood-the-machine method. Round 29/30 results shipped: #559 + #563 are closed plan:done and worktrees went 96 GB -> 29 GB. #225 and #564 still open.

STORE GROWTH IS ACCELERATING (same machine, one day apart): 143 MB/445 threads/162 ms stringify/375 ms parse -> 170 MB/545 threads/310 ms stringify/624 ms parse. Composition now 163.8 MB archived vs 4.8 MB live (97% dead weight). .bak sibling 168 MB. Posted as a table on #225; it is now the largest single main-process stall.

FILED #617 — threads:changed ships all 545 rows (0.76 MB) on every state change. pushThreadsChanged (runner.js:1813-1815) is unconditional, 59 call sites in runner.js, 6 inside the stream onEvent handler; fires on per-turn transitions (turn start/end, permission raised, verify, tit
…

### #560 did not ship: store still stringifies archived transcripts on every flush

Issue #560 ("archived transcripts are not re-stringified on every store flush") is CLOSED as a duplicate of still-open #225. electron/store.js _flushAsync (around the JSON.stringify(this.data) call) and saveNow still serialize the whole blob, including messagesByThread for archived threads. There is no transcripts/<threadId>.json split. A 0.8.0 docs brief listed #560 as shipped; do not document it as done until #225 lands.

### addProject git-inits a local folder that is not a repo (#612)

Add existing used to throw `Not a git repository: <path>. Choose a directory inside a git work tree.` Create new already mkdir + git-init, but refused when the target folder already existed — so a folder with code and no .git could not be added.

#612 / thread 35b51c2c: addProject now calls ensureGitWorkTree after the directory-exists check. If `git rev-parse --is-inside-work-tree` is not true, run `git init -q` and re-check. Init failure throws `Could not initialize a git repository: …` (not the old not-a-repo toast). Existing work trees and SSH remotes are unchanged. Files already in the folder stay untracked.

createProject is mkdir + addProject; on any add failure it rm -rf the folder it just created. Git init lives in one place.

No initial commit — worktree threads still need HEAD; t
…

### #607 transcript autoscroll: delayed scroll unsticks after thread switch and permission cards

Issue #607 (follow-up to #306/#408). ThreadView stickToBottom is flipped false by onBodyScroll whenever distance > 80px. Two sequences are not user scroll-up and used to strand the view:

1. Thread switch: App visibleDetail is null while threads.get is in flight (#83), so .body unmounts and remounts at scrollTop=0. Chrome then fires a delayed scroll; stick clears; ResizeObserver no-ops. Pin in useLayoutEffect and forceStick across null→new-thread recovery (seenThread) until a pin actually moves scrollTop.
2. Permission/question/plan card: pendingPermission is a sibling of the timeline; status stays working. A tall card (or post-pin markdown growth) plus overflow-anchoring fires scroll with distance > 80. New requestId force-sticks and re-pins. overflow-anchor:none on .body.

Do not arm for
…
