{
  "version": 1,
  "readOrder": ["critical", "impl", "tests", "docs"],
  "chunks": [
    {
      "area": "critical",
      "rationale": "Thread | Git tabs and the full-height Git pane replace the 520px overlay. Wrong flex/hidden wiring hides the transcript permanently or clips commit again.",
      "risks": [
        ".body { display:flex } can override [hidden] if .body[hidden] is missing",
        "changesOpen is lifted in App; switching threads must still close the pane",
        "Composer stays mounted while hidden so draft text is not lost"
      ]
    },
    {
      "area": "impl",
      "rationale": "Header declutter: Spec/Teach/Ask start from overflow, Notes is icon-only, overflow stays usable while a run is in progress. Git pane is file list + selected diff + pinned commit.",
      "risks": [
        "Overflow used to disable entirely while working; destructive items are now hidden instead",
        "File selection defaults to diff.files[0], which may not be the first visible itinerary row"
      ]
    },
    {
      "area": "tests",
      "rationale": "Pane tabs, Git chrome, file select, spec/teach/ask overflow, #555 commit-box pin (flex, not sticky).",
      "risks": [
        "CSS ruleBody() matcher is first-brace-wins; the 720px media query also has .changesSplit"
      ]
    }
  ],
  "risks": [
    "The reference screenshot never reached this worker; the pane is inferred from Claude Code Desktop / #552, not pixel-matched",
    "Environment still has a Changes card that opens the same pane; two entry points"
  ]
}
