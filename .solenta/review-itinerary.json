{
  "version": 1,
  "readOrder": ["docs"],
  "chunks": [
    {
      "area": "docs",
      "rationale": "Read site/changelog.html 0.8.0 first — it is the curated claim of what shipped. Then site/index.html bento/orchestration/install (surgical copy only; hero and layout untouched). Then site/docs.html: Spaces removal, Ask as its own h2, /btw, slash extras, host-tools table, suggested-work, permission edit, nightly side-by-side. Confirm the sidebar copy describes buildFlatSidebar (pinned block + snoozed/settled shelves), not the #567 Later shelf.",
      "risks": [
        "The worker brief listed #567 Later-shelf as a 0.8.0 bullet. That model is dead in Sidebar.tsx (buildFlatSidebar only). Changelog and docs describe the T3 flat list that actually ships; a reviewer expecting Later-shelf wording will think this is wrong.",
        "0.7.0 already shipped next-git-action and worktree fail-closed. Those are not re-announced. Review-bottleneck / Ask / Git-pane / spec dispatch are new and are announced.",
        "Issue numbers are omitted to match every existing site changelog entry. GitHub release notes (another worker) may still use them.",
        "Landing added three small bento cells (flat sidebar, Ask, Spec) plus a Git sentence on the itinerary cell. The #570 redesign is intact; the grid is denser.",
        "Pane workspace terminal/browser/tasks are placeholders (\"isn't built yet\") so they are not documented as shipping. Git-in-the-thread is.",
        "Install cards now hardcode Solenta-0.8.0-*.zip. That will go stale on the next release the same way 0.1.0 did."
      ]
    }
  ],
  "risks": [
    "docs.html was already updated earlier today (4e580c7) for many 0.8 features; this pass fills gaps and retires Spaces. A reviewer diffing against 0.7.0 docs will see both commits.",
    "HTML nest check and href=# vs id= passed on changelog.html, index.html, and docs.html. Visual landing layout was not checked in a browser."
  ]
}
