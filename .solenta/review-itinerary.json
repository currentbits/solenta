{
  "version": 1,
  "readOrder": ["critical", "tests", "impl"],
  "chunks": [
    {
      "area": "critical",
      "rationale": "T3 does not put the thread-actions list inside the sidebar scroll container. pingdotgg/t3code threadActionMenu.logic.ts + api.contextMenu.show → Electron Menu.popup with Snooze as children. Solenta now does the same: electron/contextMenu.js Menu.popup on Desktop, src/contextMenuFallback.ts position:fixed on document.body otherwise. The in-card nested HTML drill-in, z-index 8, and menuPlaceUp measurement are gone from ThreadCard.",
      "risks": [
        "The group-header create menu still uses the old .snoozeMenu position:absolute path and can still clip.",
        "jsdom tests exercise the HTML fallback, not Menu.popup. A native-menu regression would not fail the renderer suite."
      ]
    },
    {
      "area": "tests",
      "rationale": "test/sidebar.test.tsx #582/#583 now assert the portal is document.body + position:fixed, Snooze children are a sibling submenu, right-click opens the same menu, hover clock+settle exist. Portal queries use document.querySelector — m.query only sees the mount container. press() not pressFocused, because the portal is outside the mounted tree.",
      "risks": [
        "afterEach dismissContextMenu() is file-wide in sidebar.test.tsx. A later test that expected a leftover menu would fail; none should.",
        "Pin/snooze App tests that still used m.query for menu items were retargeted in pinSnooze, forkHandoff, threadRename."
      ]
    },
    {
      "area": "impl",
      "rationale": "src/threadActionMenu.ts is the T3 item list (Snooze children, Wake when already snoozed, Solenta Fork/Hand off/Mute kept). Hover clock opens presets only; hover settle is the check; … and row contextmenu open the full list. CoderApi.contextMenu is optional so web/devCoder keep the fallback.",
      "risks": [
        "contextMenu:show is registered outside IPC_HANDLERS so the web bridge never Menu.popup on the server. A future catch-all IPC map must not swallow that channel.",
        "This branch does not flatten the sidebar into T3's single list. Groups + Later remain."
      ]
    }
  ],
  "risks": [
    "Issue #592 tracks this. #582/#583 were plan:done on the overlay approach; the overlay was the wrong T3 copy.",
    "A parallel T3-flat-sidebar rewrite already exists on thread 8dea3d17; do not merge both blindly."
  ]
}
