{
  "version": 1,
  "readOrder": ["tests", "critical", "impl"],
  "chunks": [
    {
      "area": "tests",
      "rationale": "Read grok.test.js first: the argv contract is the whole fix (asking modes must emit bypassPermissions + --always-approve, never auto). Then providers.test.js and sandbox.test.js.",
      "risks": [
        "Runner grok tests spawn a fake binary and only check argv; grok 1.0.5 honoring --always-approve was verified live.",
        "plan mode must stay plan and not get --always-approve."
      ]
    },
    {
      "area": "critical",
      "rationale": "electron/providers.js grok.buildArgs. Asking modes used to map to --permission-mode auto, which grok 1.0.5 treats as default and auto-cancels run_terminal_command as a user stop.",
      "risks": [
        "default and acceptEdits on grok now fully auto-approve tools.",
        "Already-running grok children keep the old argv until the next spawn; the packaged Nightly must be restarted after this lands."
      ]
    },
    {
      "area": "impl",
      "rationale": "electron/sandbox.js badge reason string tracks the flags we actually pass.",
      "risks": []
    },
    {
      "area": "electron/worktrees.js — tryResolveItineraryOnly",
      "rationale": "When the sole unmerged path is .solenta/review-itinerary.json, combine ours+theirs and stage it so Merge does not stick.",
      "risks": [
        "A real conflict that includes other files still surfaces as MERGE_CONFLICT."
      ]
    }
  ],
  "risks": [
    "Packaged Solenta Nightly loads electron JS from the app bundle at start. Git merge alone does not change a running Nightly process."
  ]
}
