{
  "version": 1,
  "readOrder": ["tests", "critical", "impl"],
  "chunks": [
    {
      "area": "tests",
      "rationale": "test/conflictForecastBadge.test.tsx was an add/add-style content conflict: main added m.unmount() on the multi-pair case and a keyboard-focusable test; our branch already had that same keyboard test plus the later readability suite. Keep one keyboard test, add the missing unmount, do not drop the readability assertions that lock --card and the raised-card :has() rule.",
      "risks": [
        "If a future merge reintroduces .conflictTipLine[data-conflict-kind=overlap] { color: var(--text-muted) }, the readability test still fails closed — that is intended.",
        "Did not re-run the full electron suite after the merge, only Ask + conflict-forecast + header/create-thread renderer tests."
      ]
    },
    {
      "area": "critical",
      "rationale": "src/components/Sidebar.module.css kept our readable conflict tip: raise the open card with :has(.conflictWrap:hover/:focus-within), paint the tip on --card, and drop main's muted overlap line. That is the newer #516 overlay fix; main only had the first hover-tip landing.",
      "risks": [
        "Main's muted overlap color is gone on purpose. If someone wanted quieter overlap copy, they will see full-contrast lines.",
        "Ask mode code in Sidebar.tsx/ThreadView/runner auto-merged; I verified the ask tag, caret item, startAskRun intercept, and IPC create path survived, but I did not re-audit every auto-merged hunk in runner.js."
      ]
    },
    {
      "area": "impl",
      "rationale": ".solenta/review-itinerary.json is per-PR author notes. Keep the Ask-mode itinerary (this PR), not main's merge-PR itinerary about updatePrBranchFromBase. Incoming main also deleted quota-wait; leftover fromQuotaWait fields in startAskRun are inert.",
      "risks": [
        "Main deleted electron/quotaWait.js. Auto-merge left a few fromQuotaWait / quotaWaitUntil mentions in runner.js. They do not require the deleted module, but they are dead.",
        "A later cleanup should drop those leftovers rather than revive quota-wait on this branch."
      ]
    }
  ],
  "risks": [
    "PR #532 conflicted with origin/main after 122 commits, including #525 merge recovery, #516 conflict-pill hover, #511 worktree fail-closed, and the quota-wait deletion.",
    "No live Electron click-through after the merge. Verification: tsc --noEmit, Ask electron+renderer tests, conflictForecastBadge, sidebar/header/threadView, fork-handoff, teach/orch.",
    "Print-mode fallback still uses the provider CLI. That is cheap and no-tools, not zero subscription usage. fm is the free path."
  ]
}
