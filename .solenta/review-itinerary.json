{
  "version": 1,
  "readOrder": ["tests", "impl"],
  "chunks": [
    {
      "area": "tests",
      "rationale": "Start with test/conflictForecastBadge.test.tsx 'conflict tip readability'. Those three CSS-source assertions are the contract: opaque --card fill, card :has() z-index rise on hover/focus, no muted overlap copy. They failed on the #516 tip (background --bg-elevated, no sibling escape, --text-muted) and pass after the CSS change.",
      "risks": [
        "Tests read the CSS file, not computed paint. They cannot prove Chromium stacking against a real next card.",
        "The overlap-contrast test is a no-op if the muted rule is simply deleted; that is intentional but a weaker lock than a positive color assertion."
      ]
    },
    {
      "area": "impl",
      "rationale": "Then src/components/Sidebar.module.css around .card:has(.conflictWrap:hover) and .conflictTip. The unreadable overlay in the screenshot is this tip hanging onto the next transparent thread card; raising the open card and using --card is the actual fix. No TS/JS change.",
      "risks": [
        "CSS modules must hash both .card and .conflictWrap inside :has(). If only .card is localized the rise never applies.",
        "Kanban columns use overflow:hidden (KanbanView.module.css), so a tall tip can still clip there — pre-existing #516 geometry, not fixed here.",
        "This worktree cherry-picked 6e10ae9 (#516) onto a diverged base; a PR against origin/main should contain only the readability CSS + tests, not a second copy of #516."
      ]
    }
  ],
  "risks": [
    "No live Electron hover was driven; verification is the 17-test renderer run plus tsc --noEmit.",
    "A parent overflow (sidebar list is overflow-y auto, fine; kanban column is hidden) can still clip the tip even after the z-index rise."
  ]
}
