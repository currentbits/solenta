{
  "version": 1,
  "readOrder": ["tests", "critical", "impl"],
  "chunks": [
    {
      "area": "tests",
      "rationale": "PR #529 conflicted with origin/main after #530/#531/#532. Only content conflict was this itinerary. Re-run electron/test/config-doctor.test.js and tsc to prove the auto-merged IPC fan-out still typechecks against main's ThreadStatus (quota-wait removed) plus Vibe Kanban / Ask mode / peek additions.",
      "risks": [
        "Auto-merge of ipc.ts/useCoder/fakeCoder/devCoder can silently drop one side of a method. tsc is the check.",
        "Renderer MemoryTab tests do not remount App after the merge."
      ]
    },
    {
      "area": "critical",
      "rationale": "electron/configDoctor.js and the lint/preview/write services did not conflict. Confirm AgentConfig* types still sit above AddProjectOptions in src/shared/ipc.ts and CoderApi.projects still has lintAgentConfig/previewAgentConfig/writeAgentConfig after main's vibeKanban/ask/peek landings.",
      "risks": [
        "A leftover quota-wait mention on our side would fail tsc against main's narrowed ThreadStatus.",
        "Write still overwrites AGENTS.md/CLAUDE.md with no backup."
      ]
    },
    {
      "area": "impl",
      "rationale": "preload, ipc.js, wireClient, useCoder, App, AgentsPanel, fakeCoder, devCoder auto-merged. This itinerary is the only file we resolved by hand — keep our #412 doctor notes, drop the incoming Vibe Kanban merge notes.",
      "risks": [
        "devCoder write still throws (browser-dev only).",
        "Doctor card still hidden when the memory server is down."
      ]
    }
  ],
  "risks": [
    "Session-end auto-learning stays on #247. No fleet-wide grade rollup.",
    "This is a merge-to-main restack of #529, not a feature change."
  ]
}
