{
  "version": 1,
  "readOrder": ["tests", "critical", "impl", "docs"],
  "chunks": [
    {
      "area": "tests",
      "rationale": "PR #530 conflicted with origin/main after #531/#532. Two content conflicts: this itinerary and src/shared/ipc.ts. Resolution: keep the Vibe Kanban types, take main's ThreadStatus (quota-wait is gone). Re-run electron/test/vibe-kanban.test.js, test/vibeKanbanSection.test.tsx, ipc-seam, and tsc after the merge.",
      "risks": [
        "Fixture DBs are a subset of the real VK migration chain.",
        "ipc-seam must still register vibeKanban:* after the Ask-mode and peek handlers landed on main."
      ]
    },
    {
      "area": "critical",
      "rationale": "src/shared/ipc.ts: VibeKanbanPreview / VibeKanbanImportResult stay above ThreadStatus. ThreadStatus is idle|working|done|failed — do not resurrect quota-wait. electron/vibeKanban.js did not conflict; the importer is unchanged.",
      "risks": [
        "A leftover quota-wait mention in our branch (useCoder, fakeCoder, ThreadInfo comments) would fail tsc against the narrowed union.",
        "A VK project with several repos still keeps only the first repo path."
      ]
    },
    {
      "area": "impl",
      "rationale": "README, SettingsModal, preload, ipc.js, wireClient, useCoder, fakeCoder, devCoder auto-merged. Confirm vibeKanban:* handlers and the Your data section still sit next to Ask-mode / peek additions from main.",
      "risks": [
        "threads:changed still refreshes projects.list() so imported projects appear without a restart.",
        "Web mode still has no folder/save dialog."
      ]
    },
    {
      "area": "docs",
      "rationale": "README VK bullet and site/docs.html paragraph auto-merged next to the 0.7.0 feature list. Read last.",
      "risks": [
        "Site docs are the girder-deployed copy; this branch does not publish them."
      ]
    }
  ],
  "risks": [
    "Cards become Solenta threads, not GitHub planboard issues.",
    "Verification is the electron + renderer tests and tsc. No live Electron Settings click-through after this merge."
  ]
}
